# ─────────────────────────────────────────────────────────────────────────────
# RAG Application — Environment Variables
# Copy this file to .env and set your values.
# The .env file is gitignored and must NOT be committed.
# ─────────────────────────────────────────────────────────────────────────────

# ── Ollama settings ───────────────────────────────────────────────────────────
# URL where your Ollama server is listening
OLLAMA_BASE_URL=http://localhost:11434

# Ollama model tag to use for generation (must be pulled first: ollama pull llama3)
LLM_MODEL=llama3

# ── Embedding settings ────────────────────────────────────────────────────────
# HuggingFace sentence-transformers model name
EMBEDDING_MODEL=all-MiniLM-L6-v2

# ── Chunking ──────────────────────────────────────────────────────────────────
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# ── Retrieval ─────────────────────────────────────────────────────────────────
TOP_K=3

# ── Storage (relative to project root) ───────────────────────────────────────
DATA_DIR=data
VECTORSTORE_DIR=vectorstore

# ── CORS (comma-separated allowed origins for the FastAPI backend) ────────────
CORS_ORIGINS=http://localhost:8501
